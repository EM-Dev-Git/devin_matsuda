# トランスクリプト議事録作成API 実装計画書

## 概要
Teams会議のトランスクリプトからOpenAIを使用して議事録を自動生成するFastAPI Webアプリケーションの実装計画書です。JWT認証、データベース連携、ログ機能を含む完全なAPIシステムを構築します。

## システム要件

### 機能要件
- **議事録生成**: OpenAI APIを使用してトランスクリプトから議事録を自動生成
- **認証システム**: JWT トークンベースの認証機能
- **ユーザー管理**: データベースでのUserID/Password管理
- **ログ機能**: コンソール出力によるリクエスト/レスポンスログ
- **環境設定**: 外部化された環境変数管理

### 技術要件
- **フレームワーク**: FastAPI
- **認証**: JWT (JSON Web Token)
- **AI統合**: OpenAI API
- **データベース**: SQLite (開発用) / PostgreSQL (本番用)
- **ログ**: Python標準ログライブラリ
- **環境管理**: python-dotenv

## ディレクトリ構成

```
project/
├── .env                    # 環境変数設定
├── requirements.txt        # 依存関係
├── main.py                # FastAPIアプリケーションエントリーポイント
├── database.py            # データベース設定
├── routers/               # APIルーター
│   ├── __init__.py
│   ├── auth.py           # 認証関連エンドポイント
│   └── minutes.py        # 議事録生成エンドポイント
├── modules/               # ビジネスロジック
│   ├── __init__.py
│   ├── auth_manager.py   # JWT認証管理
│   ├── openai_client.py  # OpenAI API クライアント
│   ├── minutes_generator.py # 議事録生成ロジック
│   └── logger.py         # ログ管理
├── schemas/               # Pydanticモデル
│   ├── __init__.py
│   ├── auth.py           # 認証関連スキーマ
│   ├── user.py           # ユーザー関連スキーマ
│   └── minutes.py        # 議事録関連スキーマ
└── models/                # データベースモデル
    ├── __init__.py
    └── user.py           # ユーザーテーブルモデル
```

## データベース設計

### Userテーブル
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## API エンドポイント設計

### 認証エンドポイント
- `POST /auth/login` - ログイン（JWT トークン取得）
- `POST /auth/register` - ユーザー登録

### 議事録生成エンドポイント
- `POST /minutes/generate` - 議事録生成（JWT認証必須）

## スキーマ定義

### 認証関連
```python
# schemas/auth.py
class LoginRequest(BaseModel):
    user_id: str
    password: str

class LoginResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"

class RegisterRequest(BaseModel):
    user_id: str
    password: str
```

### 議事録関連
```python
# schemas/minutes.py
class MinutesRequest(BaseModel):
    transcript: str

class MinutesResponse(BaseModel):
    meeting_minutes: str
    generated_at: datetime
```

## セキュリティ設計

### JWT認証
- **アルゴリズム**: HS256
- **有効期限**: 24時間
- **シークレットキー**: 環境変数で管理
- **ペイロード**: user_id, exp, iat

### パスワード管理
- **ハッシュ化**: bcrypt
- **ソルト**: 自動生成
- **検証**: bcrypt.checkpw()

## 環境変数設計

```env
# .env
# データベース設定
DATABASE_URL=sqlite:///./app.db

# JWT設定
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# OpenAI設定
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-3.5-turbo

# アプリケーション設定
DEBUG=True
LOG_LEVEL=INFO
```

## ログ設計

### ログレベル
- **INFO**: 正常なリクエスト/レスポンス
- **WARNING**: 認証失敗、バリデーションエラー
- **ERROR**: API エラー、システムエラー
- **DEBUG**: 詳細なデバッグ情報

### ログ出力項目
- タイムスタンプ
- ログレベル
- エンドポイント
- ユーザーID（認証済みの場合）
- リクエストボディ
- レスポンスボディ
- 処理時間

## 実装手順

### Phase 1: 基盤構築
1. プロジェクト構造作成
2. 依存関係設定（requirements.txt）
3. 環境変数設定（.env）
4. データベース設定
5. ログ設定

### Phase 2: 認証システム
1. ユーザーモデル作成
2. JWT認証モジュール実装
3. 認証エンドポイント実装
4. パスワードハッシュ化実装

### Phase 3: 議事録生成システム
1. OpenAI クライアント実装
2. 議事録生成ロジック実装
3. 議事録生成エンドポイント実装
4. 認証ミドルウェア統合

### Phase 4: テスト・検証
1. 単体テスト実装
2. 統合テスト実行
3. セキュリティテスト
4. パフォーマンステスト

## 依存関係

```txt
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
sqlalchemy==2.0.23
python-dotenv==1.0.0
openai==1.3.7
pydantic==2.5.0
```

## セキュリティ考慮事項

1. **認証トークン**: HTTPSでの送信必須
2. **パスワード**: 最小8文字、複雑性要件
3. **レート制限**: API呼び出し制限実装
4. **入力検証**: 全入力データのバリデーション
5. **ログ**: 機密情報のマスキング

## パフォーマンス考慮事項

1. **データベース**: インデックス最適化
2. **OpenAI API**: 非同期処理実装
3. **キャッシュ**: 頻繁なクエリのキャッシュ化
4. **ログ**: 非同期ログ出力

## 運用考慮事項

1. **ヘルスチェック**: `/health` エンドポイント
2. **メトリクス**: Prometheus対応
3. **ドキュメント**: OpenAPI自動生成
4. **デプロイ**: Docker対応

## テスト戦略

### 単体テスト
- 各モジュールの個別テスト
- モックを使用したAPI テスト
- バリデーションテスト

### 統合テスト
- エンドツーエンドテスト
- データベース統合テスト
- OpenAI API統合テスト

### セキュリティテスト
- 認証バイパステスト
- SQLインジェクションテスト
- XSSテスト

## 完成予定機能

✅ **認証システム**
- ユーザー登録・ログイン
- JWT トークン認証
- セキュアなパスワード管理

✅ **議事録生成**
- OpenAI統合
- トランスクリプト解析
- 構造化された議事録出力

✅ **ログ機能**
- 包括的なリクエスト/レスポンスログ
- エラーログ
- パフォーマンスログ

✅ **セキュリティ**
- JWT認証
- パスワードハッシュ化
- 入力検証

## 状態遷移図

### システム全体の状態遷移

```mermaid
graph TD
    A[システム起動] --> B[未認証状態]
    
    B --> C{ユーザー登録?}
    C -->|Yes| D[ユーザー登録処理]
    C -->|No| E{ログイン?}
    
    D --> F{登録成功?}
    F -->|Yes| G[登録完了]
    F -->|No| H[登録エラー]
    
    G --> B
    H --> B
    
    E -->|Yes| I[ログイン処理]
    E -->|No| B
    
    I --> J{認証成功?}
    J -->|Yes| K[JWT トークン発行]
    J -->|No| L[認証エラー]
    
    K --> M[認証済み状態]
    L --> B
    
    M --> N{議事録生成要求?}
    N -->|Yes| O[トークン検証]
    N -->|No| M
    
    O --> P{トークン有効?}
    P -->|Yes| Q[議事録生成処理]
    P -->|No| R[認証エラー]
    
    Q --> S{OpenAI API利用可能?}
    S -->|Yes| T[OpenAI API呼び出し]
    S -->|No| U[デモモード処理]
    
    T --> V{API呼び出し成功?}
    V -->|Yes| W[議事録生成完了]
    V -->|No| X[API エラー]
    
    U --> Y[デモ議事録生成]
    Y --> W
    
    W --> Z[レスポンス返却]
    X --> AA[エラーレスポンス]
    R --> AA
    
    Z --> M
    AA --> M
    
    M --> BB{ログアウト?}
    BB -->|Yes| B
    BB -->|No| M
```

### 認証フローの詳細状態遷移

```mermaid
stateDiagram-v2
    [*] --> 未認証
    
    未認証 --> ユーザー登録要求 : POST /auth/register
    未認証 --> ログイン要求 : POST /auth/login
    
    ユーザー登録要求 --> 入力検証 : リクエスト受信
    入力検証 --> ユーザー存在チェック : 検証成功
    入力検証 --> 登録エラー : 検証失敗
    
    ユーザー存在チェック --> パスワードハッシュ化 : ユーザー未存在
    ユーザー存在チェック --> 登録エラー : ユーザー既存在
    
    パスワードハッシュ化 --> DB保存 : ハッシュ化完了
    DB保存 --> 登録完了 : 保存成功
    DB保存 --> 登録エラー : 保存失敗
    
    登録完了 --> 未認証 : レスポンス送信
    登録エラー --> 未認証 : エラーレスポンス送信
    
    ログイン要求 --> ログイン入力検証 : リクエスト受信
    ログイン入力検証 --> ユーザー認証 : 検証成功
    ログイン入力検証 --> ログインエラー : 検証失敗
    
    ユーザー認証 --> パスワード検証 : ユーザー存在
    ユーザー認証 --> ログインエラー : ユーザー不存在
    
    パスワード検証 --> JWT生成 : 認証成功
    パスワード検証 --> ログインエラー : 認証失敗
    
    JWT生成 --> 認証完了 : トークン生成成功
    JWT生成 --> ログインエラー : トークン生成失敗
    
    認証完了 --> 認証済み : トークン返却
    ログインエラー --> 未認証 : エラーレスポンス送信
    
    認証済み --> トークン期限切れ : 24時間経過
    トークン期限切れ --> 未認証 : 自動ログアウト
```

### 議事録生成フローの詳細状態遷移

```mermaid
stateDiagram-v2
    [*] --> 待機中
    
    待機中 --> 議事録生成要求 : POST /minutes/generate
    
    議事録生成要求 --> JWT検証 : Authorization ヘッダー確認
    JWT検証 --> トークン解析 : ヘッダー存在
    JWT検証 --> 認証エラー : ヘッダー不存在
    
    トークン解析 --> 署名検証 : トークン形式正常
    トークン解析 --> 認証エラー : トークン形式異常
    
    署名検証 --> 有効期限チェック : 署名有効
    署名検証 --> 認証エラー : 署名無効
    
    有効期限チェック --> ユーザー情報取得 : 期限内
    有効期限チェック --> 認証エラー : 期限切れ
    
    ユーザー情報取得 --> 入力検証 : ユーザー存在
    ユーザー情報取得 --> 認証エラー : ユーザー不存在
    
    入力検証 --> OpenAI設定確認 : トランスクリプト有効
    入力検証 --> 入力エラー : トランスクリプト無効
    
    OpenAI設定確認 --> OpenAI処理 : API キー設定済み
    OpenAI設定確認 --> デモ処理 : API キー未設定
    
    OpenAI処理 --> プロンプト構築 : 初期化成功
    OpenAI処理 --> API エラー : 初期化失敗
    
    プロンプト構築 --> API呼び出し : プロンプト完成
    API呼び出し --> レスポンス解析 : API成功
    API呼び出し --> API エラー : API失敗
    
    レスポンス解析 --> 議事録完成 : 解析成功
    レスポンス解析 --> API エラー : 解析失敗
    
    デモ処理 --> トランスクリプト解析 : デモモード開始
    トランスクリプト解析 --> デモ議事録生成 : 解析完了
    デモ議事録生成 --> 議事録完成 : デモ議事録作成
    
    議事録完成 --> ログ記録 : 生成成功
    ログ記録 --> レスポンス送信 : ログ完了
    レスポンス送信 --> 待機中 : 正常終了
    
    認証エラー --> エラーログ記録 : 認証失敗
    入力エラー --> エラーログ記録 : 入力検証失敗
    API エラー --> エラーログ記録 : OpenAI エラー
    
    エラーログ記録 --> エラーレスポンス送信 : ログ完了
    エラーレスポンス送信 --> 待機中 : エラー終了
```

### データベース操作の状態遷移

```mermaid
stateDiagram-v2
    [*] --> DB接続待機
    
    DB接続待機 --> セッション作成 : DB操作要求
    セッション作成 --> トランザクション開始 : セッション確立
    セッション作成 --> DB接続エラー : セッション失敗
    
    トランザクション開始 --> クエリ実行 : トランザクション成功
    トランザクション開始 --> DB接続エラー : トランザクション失敗
    
    クエリ実行 --> 結果処理 : クエリ成功
    クエリ実行 --> ロールバック : クエリ失敗
    
    結果処理 --> コミット : 処理成功
    結果処理 --> ロールバック : 処理失敗
    
    コミット --> セッション終了 : コミット成功
    コミット --> ロールバック : コミット失敗
    
    ロールバック --> セッション終了 : ロールバック完了
    
    セッション終了 --> DB接続待機 : セッション解放
    DB接続エラー --> DB接続待機 : エラー処理完了
```

### ログ処理の状態遷移

```mermaid
stateDiagram-v2
    [*] --> ログ待機
    
    ログ待機 --> ログ要求受信 : ログ出力要求
    
    ログ要求受信 --> ログレベル判定 : 要求解析
    ログレベル判定 --> ログデータ構築 : レベル適切
    ログレベル判定 --> ログ破棄 : レベル不適切
    
    ログデータ構築 --> 機密情報マスキング : データ構築完了
    機密情報マスキング --> JSON変換 : マスキング完了
    
    JSON変換 --> コンソール出力 : JSON化成功
    JSON変換 --> ログエラー : JSON化失敗
    
    コンソール出力 --> ログ完了 : 出力成功
    コンソール出力 --> ログエラー : 出力失敗
    
    ログ完了 --> ログ待機 : 正常終了
    ログエラー --> ログ待機 : エラー終了
    ログ破棄 --> ログ待機 : 破棄完了
```

この実装計画に基づいて、段階的に開発を進めていきます。
